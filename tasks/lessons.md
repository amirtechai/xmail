# Xmail — Lessons Learned
# Updated per session. Read at start of every session.

## Session 1 (2026-04-19) — Project Setup
- (No lessons yet — project just started)

## Rules to Follow
- Always check todo.md before starting work
- Run `docker-compose config` after any compose change
- Run `alembic upgrade head` after model changes
- LIA (Legitimate Interest Assessment) is mandatory — never skip
- Email sending requires explicit user action — never auto-send
